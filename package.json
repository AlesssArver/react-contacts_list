{
  "name": "react-contacts",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/AlesssArver/react-contacts_list.git",
  "author": "AlessArver <alessarver@gmail.com>",
  "license": "MIT",
  "scripts": {
    "server:start": "yarn server --prefix server",
    "server:build": "yarn build --prefix server",
    "server:watch": "yarn watch --prefix server",
    "server": "yarn watch --prefix server && yarn server --prefix server",
    "client:start": "yarn start --prefix client",
    "client:install": "yarn install --prefix client",
    "client:build": "yarn build --prefix client",
    "dev": "concurrently \\\"yarn server:start\\\" \\\"yarn client:start\\\""
  }
}
